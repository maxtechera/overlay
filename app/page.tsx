export default function Home() {
  return (
    <main style={{ padding: 32, fontFamily: "system-ui", color: "#eee", background: "#111", minHeight: "100vh" }}>
      <h1>Overlay</h1>
      <p>Any-site A/B variant builder, driven by an agent. Building in the open — see PRD.md.</p>
      <p>M1 (hero tracer) is in progress: chat + live preview land here.</p>
    </main>
  );
}
